# Social preview

GitHub has no API for the social preview image, so this one is uploaded by hand — once.

1. Go to **Settings → General → Social preview** on `github.com/neelbarm/sweep`.
2. Click **Edit → Upload an image** and pick `docs/social.png` from this repo (1280×640).
